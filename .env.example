# --- Database ---
DATABASE_URL=postgres://agent:agent@localhost:5432/agent
# --- Cache / queues ---
REDIS_URL=redis://localhost:6379
# --- Auth ---
JWT_SECRET=change-me-to-a-long-random-string
# --- Secrets vault (envelope encryption master key, 32 bytes base64 — `openssl rand -base64 32`) ---
VAULT_MASTER_KEY=
# --- Model providers ---
ANTHROPIC_API_KEY=
XAI_API_KEY=
# --- API ---
API_PORT=4000
API_URL=http://localhost:4000
APP_URL=http://localhost:3000
# --- Google (Gmail + Calendar) OAuth client — see README "Connecting Google" ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# --- WhatsApp Business Platform — see README "Connecting WhatsApp" ---
WHATSAPP_APP_SECRET=
WHATSAPP_WEBHOOK_VERIFY_TOKEN=
# --- Web (control center) ---
NEXT_PUBLIC_API_URL=http://localhost:4000
